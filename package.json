{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "nodev": "nodemon app.js",
    "build": "postcss public/styles/tailwind.css -o public/styles/style.css",
    "watch": "postcss public/styles/tailwind.css -o public/styles/style.css --watch",
    "dev": "concurrently --kill-others \"npm run watch\" \"npm run nodev\"",
    "start": "node app.js"
  },
  "dependencies": {
    "autoprefixer": "^10.4.13",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "postcss": "^8.4.21",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.2.4"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
